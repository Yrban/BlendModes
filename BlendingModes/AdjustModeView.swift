import SwiftUI

// AdjustModeView is now folded into LayerRowView directly.
// This file is kept as a stub so any remaining references compile.
struct AdjustModeView: View {
    @Binding var layer: Layer
    var body: some View { EmptyView() }
}
